# Claude Code working directory — never needed in the image
.claude/

# Large directories
oncopig/
temperaturedata/
venogram/
vesseldata/
examplemesh/
couplemesh/

# Root-level Python / analysis scripts not used by the solver
# (/*.py matches only the build-context root, not pipeline/ subdirectory)
/*.py
*.csv
*.yaml
*.sql
*.pvsm

# Git metadata
.git/

# tutorials/ and pipeline/ are needed for the build
